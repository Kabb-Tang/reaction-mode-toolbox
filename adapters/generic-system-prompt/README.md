# 通用知识上传包

适用于支持“系统说明 + 多个文档附件”的对话环境。

1. 上传 `SYSTEM_PROMPT.md`。
2. 上传 `core/` 与 `knowledge/` 的全部文件。
3. 若平台允许设置入口提示，使用：`请按反应模式工作台帮助我处理当前情况。`

若平台只能粘贴一段文本，请使用构建后的 `dist/generic-knowledge-bundle/START_HERE.md`，并同时上传该发行目录中的 `core/` 和 `knowledge/`。
