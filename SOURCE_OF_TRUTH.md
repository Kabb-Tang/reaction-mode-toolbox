# Source of Truth

本仓库的正式维护入口如下：

1. `core/`：全局行为、安全、路由与输出约束。
2. `knowledge/`：可按需读取的当前内容真源。
3. `skills/`：每个可分发能力的入口指令；其 `references/` 为从前两层生成的副本。

`dist/` 是可再生成的发行产物，不能手工修改。`adapters/` 只能处理不同宿主的装载方式，不能承载业务逻辑或安全规则。

根目录原始 Markdown 和 `agent/` 目录在当前版本仅作为迁移基线保留。除非专门做回溯修订，不应再把它们当作新的维护位置。
