# 反应模式工作台工具箱维护规则

- 所有平台无关的行为规则写入 `core/`，不要写进宿主适配文件。
- 知识正文只维护在 `knowledge/`；不要手工修改 `skills/*/references/` 或 `dist/`。
- 每次调整场景判断、边界或话术前，先补充或核对 `evals/` 中对应案例。
- 不将普通实用问题自动转化为内在模式工作。
- 急性峰值、即时危险、严重解离、自伤或他伤信号优先稳定与转介，不进行穿透、训练或建设工作。
- `关系安全`专题目前是占位状态；只允许使用其明确写出的最小停损与转介规则，不得补写未经审核的详细处置方案。
